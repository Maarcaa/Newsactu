<div class="row">
    <p class="d-flex flex-wrap mx-auto">
    
    {% for category in categories %}

        <span><a href="{{ path('show_articles_from_category', {'alias': category.alias}) }}" class="text-decoration-none text-warning">{{ category.name }}</a></span>
        
    {% endfor %}

    </p>

</div>